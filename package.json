{
  "name": "asn-newsletter-tool",
  "version": "1.0.0",
  "description": "ASN Newsletter Tool — Generate HTML listing blocks for guest newsletters",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
